{
  "name": "wtfparcel",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist && parcel serve src/index.html --no-source-maps",
    "build": "rm -rf dist && parcel build src/index.html --no-source-maps --public-url ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fontsource/lato": "^4.2.2",
    "@fontsource/montserrat": "^4.2.2",
    "parcel": "1.12.3",
    "posthtml": "^0.15.1",
    "posthtml-include": "^1.6.2"
  },
  "devDependencies": {
    "less": "^4.1.1"
  }
}
